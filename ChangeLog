For the latest news, visit the SparForte web site.

CHANGES SINCE 2.3

1. Fix: The prompt script now reports any errors when it runs.  The prompt script uses the default prompt on an error instead of being a blank string.

